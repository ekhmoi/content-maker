{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: WebSocket Server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "args": ["serve", "-o", "./outputs"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}